<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Node Ecommerce | Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full bg-gray-400">
  <nav class="max-w-[1300px] h-[80px] mx-auto px-4 flex justify-between items-center">
    <div class="flex">
      <img class="w-[100px] " src="./logo.png" alt="Logo Ecommerce">      
    </div>
    <p class="text-2xl font-bold">Bienvenido/a <%= user.nombre  %></p>
    <div class="flex items-center">
      <a class='text-xl font-bold hover:text-gray-600' href="/logout">
        <div class='w-[40px] h-[40px] bg-red-500 hover:bg-red-300 duration-300 hover:shadow-lg flex justify-center items-center rounded-full'>
          <img class='w-[25px] h-[25px]' src="./close-session.png" alt="Close">
        </div>
      </a>
      <% if (user.isAdmin === 'S') { %>        
        <a href="#">
          <img class='w-[50px] h-[50px] object-contain' src="./profile.png" alt="Profile">
        </a>
      <% } %>
    </div>
  </nav>
  <div class="max-w-[1300px] h-[80px] mx-auto px-4 flex justify-between items-center">
    <% if (cantidad == 0) { %>
      <p class="text-2xl font-bold">No ha agregado nada al carrito.</p> 
    <% } else {%>
      <p class="text-2xl font-bold">Su carrito tiene <%= result.productos.length %>  productos.</p>       
    <% } %>
  </div>

  <div class="max-w-[1300px] mx-auto mt-2">
    <% if (cantidad !== 0) { %>
      <div class="w-full grid grid-cols-3 gap-6">
        <% for( let i = 0; i < result.productos.length; i++ ) { %>
          <div class="border border-black p-2 rounded-md hover:shadow-sm hover:shadow-black/80 hover:cursor-pointer hover:scale-105 duration-200">  
            <p class="font-bold"> <%= result.productos[i].nombre %>  </p>
            <p class="font-bold"> <%= result.productos[i].descripcion %>  </p>
            <p class="font-bold"> Precio: <%= result.productos[i].price %>  </p>
            <p class="font-bold"> Cantidad: <%= result.productos[i].quantity %>  </p>
          </div>
        <% } %>
      </div>
    <% } %>
  </div>
</body>
</html>